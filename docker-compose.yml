version: '3.8'

services:
  frontend:
    build: ./src
    ports:
      - "8443:443"
      - "8080:80"
    depends_on:
      - backend

  backend:
    build: ./src/backend
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=development
