services:
  frontend:
    build: ./client
    ports:
      - "8443:443"
      - "8080:80"
    depends_on:
      - backend

  backend:
    build: ./server
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=development
      - DATABASE_PATH=/app/database/data/database.db
    volumes:
      - ./server/database/data:/app/database/data
      - ./server/database/uploads:/app/database/uploads
